import React from 'react';

const TestAddNewCharacterContainer = props => {
    return (
        <button className="test_addNewCharacterButton" onClick={props.onClick}>Test_Add</button>
    );
};

export default TestAddNewCharacterContainer;