import _ from 'lodash';
import { combineReducers } from "redux";

export const selectCharacter = (previousState = 0, action) => {
    
};

const OmaeReducer = combineReducers(
    selectedCharacter
);

export default OmaeReducer;